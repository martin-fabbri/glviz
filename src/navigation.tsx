import * as React from 'react';
import { NavLink } from 'react-router-dom';

const Navigation = () => {
    return (
        <div>
            <NavLink to={'/'}>01 Simple Rect</NavLink>
        </div>
    );
};

export default Navigation;